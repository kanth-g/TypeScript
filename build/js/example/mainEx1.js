"use strict";
let userName = "Srikanth";
console.log(userName);
/* for run ts file use command :- tsc <file name>  or tsc <filename> -w (watch updates on the file)
    when you run this command it will genarate one js file with old(es5) javascricpt complation code
    means not comatable with modren browsers to over come this we created one file known as config.js file

    using config.js file set root directory ts file and set store complied file
*/
